export function Logo({ className = "w-6 h-6" }: { className?: string }) {
  return (
    <img
      src="/urlstash_transparentbackground copy.png"
      alt="UrlStash Logo"
      className={className}
    />
  );
}
